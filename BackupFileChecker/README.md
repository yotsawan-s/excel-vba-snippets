# Backup File Checker

ตรวจสอบว่ามีไฟล์ Backup ครบถ้วนตาม Pattern ที่กำหนดหรือไม่

## 📋 คุณสมบัติ

- ✅ ตรวจสอบโครงสร้าง Folder: `MIS > ProjectCode_xxxx > Document`
- ✅ รองรับ Pattern หลายรูปแบบ (เช่น P1_*_V*.*.xlsx, D3_*_V*.*.docx, R2_*_V*.*.pdf)
- ✅ แสดงผลเป็นตาราง พร้อมสีสัญญาณ (เขียว=OK, เหลือง=ไม่พบไฟล์, แดง=Folder ไม่พบ)
- ✅ รองรับ Project Code หลายรายการ

## 🚀 วิธีใช้งาน

### 1. Import Module เข้า Excel
1. เปิด Excel และกด `Alt + F11` เพื่อเข้า VBA Editor
2. ไปที่ `File > Import File...`
3. เลือกไฟล์ `BackupFileChecker.bas`

### 2. ปรับแต่งค่าตามต้องการ

```vba
' แก้ไข Path หลัก
mainPath = "C:\Backup\"

' แก้ไขรายการ Project Code
projectCodes = Array("TP001", "TP002", "TP003")

' แก้ไข File Patterns (9 รูปแบบ)
filePatterns = Array( _
    "P1_*_V*.*.xl??", _  ' Excel: P1_xxxx_V1.0.xlsx
    "P2_*_V*.*.xl??", _
    "P3_*_V*.*.xl??", _
    "D1_*_V*.*.xl??", _
    "D2_*_V*.*.xl??", _
    "D3_*_V*.*.do??", _  ' Word: D3_xxxx_V1.0.docx
    "R1_*_V*.*.xl??", _
    "R2_*_V*.*.pd?", _   ' PDF: R2_xxxx_V1.0.pdf
    "M1_*_V*.*.xl??" _
)
```

### 3. รันโค้ด
1. กด `Alt + F8`
2. เลือก `CheckBackupFiles`
3. กด **Run**

## 📊 ผลลัพธ์

จะสร้าง Sheet ใหม่ชื่อ **"BackupCheckResult"** แสดงผลดังนี้:

| Project Code | Pattern | File Found | File Path | Status |
|--------------|---------|------------|-----------|---------|
| TP001 | P1_*_V*.*.xl?? | P1_Report_V1.0.xlsx | C:\Backup\MIS\TP001_Project\Document\... | ✓ OK |
| TP001 | P2_*_V*.*.xl?? | Not Found | C:\Backup\MIS\TP001_Project\Document\ | ❌ MISSING |
| TP002 | P1_*_V*.*.xl?? | P1_Data_V2.1.xlsm | C:\Backup\MIS\TP002_System\Document\... | ✓ OK |

## 🎨 สีสัญญาณ

- 🟢 **สีเขียว** = พบไฟล์ตาม Pattern (✓ OK)
- 🟡 **สีเหลือง** = ไม่พบไฟล์ตาม Pattern (❌ MISSING)
- 🔴 **สีแดง** = Folder ไม่พบ (❌ ERROR)

## 📂 โครงสร้าง Path

```
C:\Backup\
└── MIS\
    ├── TP001_ProjectName\
    │   └── Document\
    │       ├── P1_Report_V1.0.xlsx
    │       ├── P2_Data_V1.5.xlsx
    │       └── ...
    └── TP002_SystemName\
        └── Document\
            └── ...
```

## 🔍 Pattern Wildcards

- `*` = ตัวอักษรอะไรก็ได้ (หลายตัว)
- `?` = ตัวอักษรใดๆ 1 ตัว
- `xl??` = รองรับทั้ง .xlsx, .xlsm, .xls
- `do??` = รองรับทั้ง .docx, .doc
- `pd?` = รองรับ .pdf

## 📝 ตัวอย่าง Pattern

```
P1_*_V*.* = P1_Report_V1.0, P1_Summary_V2.5
D3_*_V*.*.docx = D3_Manual_V1.0.docx
R2_*_V*.*.pdf = R2_Result_V3.2.pdf
```

## ⚙️ Functions

| Function | คำอธิบาย |
|----------|---------|
| `CheckBackupFiles()` | ฟังก์ชันหลักในการรัน |
| `CheckProjectBackup()` | ตรวจสอบแต่ละ Project |
| `FolderExists()` | ตรวจสอบว่า Folder มีอยู่หรือไม่ |
| `FindProjectFolder()` | ค้นหา Folder ที่ขึ้นต้นด้วย Project Code |
| `FindFileByPattern()` | ค้นหาไฟล์ตาม Pattern |
| `IsFileMatchPattern()` | ตรวจสอบไฟล์ด้วย Regex (สำรองไว้) |

## 🐛 Troubleshooting

**ปัญหา**: แสดง "MIS Folder Not Found"
- **แก้ไข**: ตรวจสอบว่า `mainPath` ถูกต้อง และมี Folder "MIS" อยู่จริง

**ปัญหา**: แสดง "Project Folder Not Found"
- **แก้ไข**: ตรวจสอบว่ามี Folder ที่ขึ้นต้นด้วย Project Code (เช่น "TP001_xxxx")

**ปัญหา**: แสดง "Document Folder Not Found"
- **แก้ไข**: ตรวจสอบว่ามี Sub Folder ชื่อ "Document" ภายใน Project Folder

## 📄 License

MIT License - Free to use and modify
