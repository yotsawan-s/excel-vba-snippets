# Date Formatting 📅

VBA modules สำหรับจัดการ date format ที่ถูกต้องใน Excel

## English Summary

This module provides functions to handle date formatting issues when working with Excel across different PC configurations. It solves the common problem where `WorksheetFunction.Text()` returns text instead of actual date values, and ensures consistent date formatting regardless of regional settings.

---

## ปัญหาที่พบ

### ปัญหาหลัก
1. **Text ไม่ใช่ Date**: การใช้ `WorksheetFunction.Text(Now(), "[$-409]dd/mm/yyyy")` จะได้ผลลัพธ์เป็น text ไม่สามารถนำไปคำนวณหรือเรียงลำดับได้
2. **Format ต่างกันระหว่าง PC**: แต่ละเครื่องอาจมี regional settings ต่างกัน ทำให้วันที่แสดงผลไม่ตรงกัน
3. **การเรียงลำดับผิดพลาด**: ข้อมูลที่เป็น text จะเรียงลำดับแบบ text (alphabetical) ไม่ใช่แบบ date

### วิธีแก้ปัญหา
1. เก็บค่าเป็น Date value จริง (ไม่ใช่ text)
2. ใช้ NumberFormat แยกจากค่าข้อมูล
3. ใช้ locale code `[$-409]` เพื่อบังคับ format แบบ English

---

## Functions ที่มีให้

### 1. TextToDate()
แปลง text date เป็น date value

**ข้อดี:**
- ใช้งานง่าย สามารถใช้กับ text date ที่มีอยู่แล้ว
- แปลง text กลับเป็น date จริงเพื่อใช้คำนวณได้

### 2. InsertFormattedDate()
ลงวันที่พร้อม format ที่ถูกต้องในเซลล์ที่ระบุ

**ข้อดี:**
- ใส่ค่าเป็น Date value จริง (ไม่ใช่ text)
- กำหนด format แยก ทำให้สามารถคำนวณและเรียงลำดับได้
- รองรับทั้งวันที่เดียว (Date) และวันที่พร้อมเวลา (Now)

### 3. ConvertRangeTextToDate()
แปลงช่วงข้อมูลที่เป็น text date ให้เป็น date value

**ข้อดี:**
- แปลงข้อมูลทั้ง range พร้อมกัน
- ตรวจสอบเฉพาะเซลล์ที่เป็น text date เท่านั้น
- กำหนด format ให้อัตโนมัติ

---

## วิธีการนำไปใช้

### วิธีที่ 1: Import Module
1. เปิด VBA Editor (Alt + F11)
2. คลิกขวาที่ VBAProject → Import File...
3. เลือกไฟล์ `DateFormatting.bas`

### วิธีที่ 2: Copy Code
1. เปิด VBA Editor (Alt + F11)
2. Insert → Module
3. Copy โค้ดจาก `DateFormatting.bas` ไปวาง

### การใช้งาน
- เรียกใช้ function ผ่าน VBA code หรือ
- เรียกใช้ `TextToDate()` เป็น UDF ใน cell ได้เลย

---

## หมายเหตุสำคัญ

### เกี่ยวกับ Locale Code
- `[$-409]` คือ locale code สำหรับ English (US)
- ใช้เพื่อบังคับให้แสดงผลเป็นรูปแบบ English โดยไม่ขึ้นกับ regional settings
- ช่วยให้ format เหมือนกันทุกเครื่อง

### ความแตกต่างระหว่าง Date และ Text
| คุณสมบัติ | Date Value | Text |
|-----------|-----------|------|
| เรียงลำดับ | ✅ ถูกต้อง | ❌ ผิดพลาด |
| คำนวณ | ✅ ได้ | ❌ ไม่ได้ |
| สูตร Date | ✅ ใช้ได้ | ❌ ต้องแปลงก่อน |
| แสดงผล | ปรับ format ได้ | แสดงตามที่เก็บ |

---

## Requirements

- Excel 2007 หรือใหม่กว่า
- ไฟล์ต้องบันทึกเป็น `.xlsm` (macro-enabled)
